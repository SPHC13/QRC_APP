<div class="qr">
        <table>
        <% @qr.modules.each_index do |x| %>
          <tr>
          <% @qr.modules.each_index do |y| %>
           <% if @qr.dark?(x,y) %>
            <td class="black"/>
           <% else %>
            <td class="white"/>
           <% end %>
          <% end %>
          </tr>
        <% end %>
        </table>
  </div>




#This is the begiining of the qr_code view
<style type="text/css">
table {
  border-width: 0;
  border-style: none;
  border-color: #0000ff;
  border-collapse: collapse;
}
td {
  border-width: 0;
  border-style: none;
  border-color: #0000ff;
  border-collapse: collapse;
  padding: 0;
  margin: 0;
  width: 10px;
  height: 10px;
}
td.black { background-color: #000; }
td.white { background-color: #fff; }
</style>



<h1>QrCodes#new</h1>
<p> This QR Application is to aid the Engineering scavenger hunt <p>
<p>Find me in app/views/qr_codes/HelloWorld.html.erb</p>
~                                                                               
"app/views/qr_codes/new.html.erb" 48L, 934C    
